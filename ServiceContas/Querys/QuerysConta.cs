using ContaDTOs;

namespace ServiceContas.Querys
{
    public static class QuerysConta
    {
        //public static async Task<TabelaConta> GetContaAtual(string idCustomer)
        //{
        //    var repoContasAWS = new RepoContasAWS();
        //    var lista = await repoContasAWS.ObterContasDoCustomer(idCustomer);

        //    var contaAtual = lista.FirstOrDefault();

        //    if (contaAtual == null) { throw new Exception("Não tem conta"); }

        //    return contaAtual;
        //}
    }

    
}
